{
  "name": "asiatechnostroy",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.11.1",
  "description": "AsiaTechnoStroy website and Cloudflare Worker lead endpoint",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check public/pricing.js && node --check public/script.js && node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
